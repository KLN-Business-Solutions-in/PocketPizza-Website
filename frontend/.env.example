# Copy this file to .env.local for local frontend work.
# The live backend is the default; set this to true to use MSW fixtures.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
NEXT_PUBLIC_USE_MOCKS=false
